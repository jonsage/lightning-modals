<aura:component implements="flexipage:availableForAllPageTypes">
	<lightning:overlayLibrary aura:id="overlayLib" />
    <aura:handler event="s2cor:OpenCustomModal" action="{!c.handleShowModal}"/>
    
    
    <aura:registerEvent name="openCustomModal" type="s2cor:OpenCustomModal"/>
	<lightning:button name="modal" label="Show Modal" onclick="{!c.handleClickModal}"/>
	<lightning:button name="wizard" label="Show Wizard" onclick="{!c.handleClickWizard}"/>
</aura:component>